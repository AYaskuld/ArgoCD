apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: launch-monitor-application
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'git@github.com:AYaskuld/K8s_monitor.git'
    targetRevision: HEAD
    path: launch
  destination: 
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
